[project]
name = "django-starter"
version = "0.1.0"
description = "A Django starter project"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "django>=4.2",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-django",
]

[tool.setuptools]
py-modules = []
